#include <stdio.h>

/**
 * funtion - various size type
 *
 * return 0
 */
int main(void)
{
	int n;
	if sizeof (num(n) = sizeof(int);
			printf(" size of char \n", n);
			printf(" size of int \n", n);
			printf(" size of long int \n", n);
			printf(" size of long long \n", n);
			printf(" size of float \n", n);

			return (0);
			

			
